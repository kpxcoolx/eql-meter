# Docs screenshots

| File | Use |
|------|-----|
| `main-window.png` | Hero shot for README + guides (fights + combat + breakdown) |

One image is enough. Prefer a clean fight list (no stray pet-named entries). Replace this file in place so markdown links stay valid.
